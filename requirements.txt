# 后端 Web 框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sse-starlette>=1.8.0

# 后端 Agent 编排
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0

# 后端 LLM (DashScope 兼容 OpenAI 接口)
openai>=1.60.0

# 后端 MCP 协议
mcp>=1.0.0
pydantic>=2.6.0

# 后端 向量记忆
chromadb>=0.5.0

# 后端 数据处理
pandas>=2.2.0
matplotlib>=3.8.0
numpy>=1.26.0

# 后端 工具依赖
duckduckgo-search>=6.0.0

# 后端 评测框架（需MSVC编译工具，Windows上可选安装）
# ragas>=0.2.0
# datasets>=3.0.0
